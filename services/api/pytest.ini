[pytest]
testpaths = tests
addopts = -q
# Cache hors de /app : compatible montage en lecture seule et conteneur non-root.
cache_dir = /tmp/.pytest_cache
# Bruit des dépréciations de dépendances tierces (passlib/crypt, jose, influxdb-client).
filterwarnings =
    ignore::DeprecationWarning
